<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis mapper for Product  Auto generated by fms-mybatis-generator -->
<mapper namespace="com.rongyi.hla.mapper.product.ProductMapper">
    <resultMap id="product" type="com.rongyi.model.product.Product">
        <id property="id" column="p_id" javaType="long" jdbcType="BIGINT"/>
        <result property="name" column="p_name" javaType="string" jdbcType="VARCHAR"/>
        <result property="createTime" column="p_create_time" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="p_update_time" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="sqlProductColumns">
		p_.id AS p_id,
		p_.`name` AS p_name,
		p_.create_time AS p_create_time,
		p_.update_time AS p_update_time
	</sql>

    <delete id="delete" parameterType="long">
        DELETE FROM t_product where  id=#{id}
    </delete>

    <select id="get" parameterType="long" resultMap="product">
        SELECT
        <include refid="sqlProductColumns"/>
        FROM t_product p_
        WHERE p_.id = #{id,jdbcType=BIGINT}
    </select>


    <select id="list" resultMap="product">
        SELECT
        <include refid="sqlProductColumns"/>
        FROM t_product p_
    </select>

    <select id="listPage" parameterType="com.rongyi.query.product.ProductQuery" resultMap="product">
        SELECT
        <include refid="sqlProductColumns"/>
        FROM t_product p_
    </select>

    <select id="getCount" parameterType="com.rongyi.query.product.ProductQuery" resultType="java.lang.Integer">
		SELECT  COUNT(id)        FROM t_product
	</select>

    <select id="getTotalCount" resultType="java.lang.Integer">
	  SELECT  COUNT(id)        FROM pull_log
	</select>

    <insert id="save" parameterType="com.rongyi.model.product.Product" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO t_product (
			`name`,
			create_time
		)
        VALUES (
			#{name,jdbcType=VARCHAR},
			now()
		)
	</insert>

    <update id="update" parameterType="com.rongyi.model.product.Product">
        UPDATE t_product
        <set>
            <if test="name != null and name != ''">`name` = #{name,jdbcType=VARCHAR},</if>
            <if test="updateTime != null">update_time = #{updateTime,jdbcType=TIMESTAMP},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <delete id="dropTable">
        DROP TABLE IF EXISTS pull_log;
    </delete>

    <delete id="createTable">
        CREATE TABLE `pull_log` (
          `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '日志主键',
          `success` smallint(4) DEFAULT NULL COMMENT '是否成功',
          `type` tinyint(4) DEFAULT NULL COMMENT '状态码',
          `name` varchar(100) DEFAULT NULL COMMENT '操作名称',
          `target_address` varchar(100) DEFAULT NULL COMMENT '操作的目标地址',
          `operator` varchar(100) DEFAULT NULL COMMENT '操作名称',
          `remark` varchar(225) DEFAULT NULL COMMENT '操作备注',
          `url` varchar(225) DEFAULT NULL COMMENT '操作者的地址',
          `create_time` datetime DEFAULT NULL COMMENT '创建时间',
          PRIMARY KEY (`id`),
          KEY `id` (`id`) USING BTREE
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='产品表';

    </delete>

</mapper>
