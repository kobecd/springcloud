<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis mapper for product_group  Auto generated by fms-mybatis-generator -->
<mapper namespace="com.rongyi.hla.mapper.product.ProductGroupMapper">
    <resultMap id="product_group" type="com.rongyi.model.product.ProductGroup">
        <id property="id" column="pg_id" javaType="int" jdbcType="INTEGER"/>
        <result property="code" column="pg_code" javaType="string" jdbcType="VARCHAR"/>
        <result property="parentCode" column="pg_parent_code" javaType="string" jdbcType="VARCHAR"/>
        <result property="name" column="pg_name" javaType="string" jdbcType="VARCHAR"/>
        <result property="image" column="pg_image" javaType="string" jdbcType="VARCHAR"/>
        <result property="typeZhName" column="pg_typeZhName" javaType="string" jdbcType="VARCHAR"/>
        <result property="notes" column="pg_notes" javaType="string" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="sqlproduct_groupColumns">
		pg_.id AS pg_id,
		pg_.code AS pg_code,
		pg_.parent_code AS pg_parent_code,
		pg_.`name` AS pg_name,
		pg_.image AS pg_image,
		pg_.typeZhName AS pg_typeZhName,
		pg_.notes AS pg_notes
	</sql>

    <select id="get" parameterType="int" resultMap="product_group">
        SELECT
        <include refid="sqlproduct_groupColumns"/>
        FROM product_group pg_
        WHERE pg_.id = #{id,jdbcType=INTEGER}
    </select>

    <select id="list" resultMap="product_group">
        SELECT
        <include refid="sqlproduct_groupColumns"/>
        FROM product_group pg_
    </select>

    <insert id="save" parameterType="com.rongyi.model.product.ProductGroup" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO product_group (
			code,
			parent_code,
			`name`,
			image,
			typeZhName,
			notes
		)
        VALUES (
			#{code,jdbcType=VARCHAR},
			#{parentCode,jdbcType=VARCHAR},
			#{name,jdbcType=VARCHAR},
			#{image,jdbcType=VARCHAR},
			#{typeZhName,jdbcType=VARCHAR},
			#{notes,jdbcType=VARCHAR}
		)
	</insert>

    <update id="update" parameterType="com.rongyi.model.product.ProductGroup">
        UPDATE product_group
        <set>
            <if test="code != null and code != ''">code = #{code,jdbcType=VARCHAR},</if>
            <if test="parentCode != null and parentCode != ''">parent_code = #{parentCode,jdbcType=VARCHAR},</if>
            <if test="name != null and name != ''">`name` = #{name,jdbcType=VARCHAR},</if>
            <if test="image != null and image != ''">image = #{image,jdbcType=VARCHAR},</if>
            <if test="typeZhName != null and typeZhName != ''">typeZhName = #{typeZhName,jdbcType=VARCHAR},</if>
            <if test="notes != null and notes != ''">notes = #{notes,jdbcType=VARCHAR},</if>
        </set>
        WHERE id = #{id,jdbcType=INTEGER}
    </update>

</mapper>
