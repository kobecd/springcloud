<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis mapper for SysUser  Auto generated by fms-mybatis-generator -->
<mapper namespace="com.arc.security3.mapper.UserMapper">
    <resultMap id="sysUser" type="com.arc.security3.domain.sys.User">
        <id property="id" column="user_id" javaType="long" jdbcType="BIGINT"/>
        <result property="username" column="user_username" javaType="string" jdbcType="VARCHAR"/>
        <result property="password" column="user_password" javaType="string" jdbcType="VARCHAR"/>
        <result property="state" column="user_state" javaType="int" jdbcType="INTEGER"/>
        <result property="createDate" column="user_create_date" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
        <result property="updateDate" column="user_update_date" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="sqlSysUserColumns">
		user_.id AS user_id,
		user_.username AS user_username,
		user_.password AS user_password,
		user_.state AS user_state,
		user_.create_date AS user_create_date,
		user_.update_date AS user_update_date
	</sql>

    <select id="get" parameterType="long" resultMap="sysUser">
        SELECT
        <include refid="sqlSysUserColumns"/>
        FROM t_sys_user user_
        WHERE user_.id = #{id,jdbcType=BIGINT}
    </select>
    <select id="getByUsername" parameterType="string" resultMap="sysUser">
        SELECT
        <include refid="sqlSysUserColumns"/>
        FROM t_sys_user user_
        WHERE user_.username = #{username}
    </select>

    <insert id="save" parameterType="com.arc.security3.domain.sys.User" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO t_sys_user (
			username,
			password,
			state,
			create_date,
			update_date
		)
        VALUES (
			#{username,jdbcType=VARCHAR},
			#{password,jdbcType=VARCHAR},
			#{state,jdbcType=INTEGER},
			#{createDate,jdbcType=TIMESTAMP},
			#{updateDate,jdbcType=TIMESTAMP}
		)
	</insert>

    <update id="update" parameterType="com.arc.security3.domain.sys.User">
        UPDATE t_sys_user
        <set>
            <if test="username != null and username != ''">username = #{username,jdbcType=VARCHAR},</if>
            <if test="password != null and password != ''">password = #{password,jdbcType=VARCHAR},</if>
            <if test="state != null">state = #{state,jdbcType=INTEGER},</if>
            <if test="createDate != null">create_date = #{createDate,jdbcType=TIMESTAMP},</if>
            <if test="updateDate != null">update_date = #{updateDate,jdbcType=TIMESTAMP},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

</mapper>
